# Environment Variables for OpenAPI Client

# API Base URL
# The generated OpenAPI client will use this as the base URL for all requests
# Make sure this points to your FastAPI backend
VITE_API_URL=http://localhost:8000

# Alternative for production:
# VITE_API_URL=https://api.luxefurniture.com

# Note: Vite requires the VITE_ prefix for environment variables
# to be exposed to the client-side code
